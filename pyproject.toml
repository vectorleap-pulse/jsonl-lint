[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "jsonl-lint"
version = "0.1.0"
description = "Zero-dependency validator that reports every malformed line in a JSONL file, not just the first."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
keywords = ["jsonl", "ndjson", "json", "lint", "validation", "dataset"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Topic :: Software Development :: Quality Assurance",
]

[project.scripts]
jsonl-lint = "jsonl_lint.cli:main"

[project.optional-dependencies]
dev = ["pytest>=7", "ruff==0.16.4"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
# Selected explicitly, and the ruff version is pinned in the dev extra.
# Relying on ruff's defaults means a release that widens them turns a green
# build red without anything in this repo changing.
select = ["E", "F", "I", "UP", "B"]

[tool.pytest.ini_options]
testpaths = ["tests"]
